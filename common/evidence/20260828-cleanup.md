# 2026-08-28 approved migration cleanup

## Scope and approval

- The two external Agent Runtime consumers were explicitly approved for deletion:
  - `projects/776113568960/locations/us-central1/reasoningEngines/2779698985680502784` (`20260822-agent-gateway-claude-identity`)
  - `projects/776113568960/locations/us-central1/reasoningEngines/382095134059134976` (`20260822-agent-gateway-claude-local-webfetch`)
- The saved Terraform destroy plan was explicitly approved after review:
  `/tmp/common-migration-20260822-destroy-final.tfplan`.

## Executed operations

- Vertex AI REST DELETE for Runtime `2779698985680502784`: HTTP 200, completed operation `3403295548063612928`.
- Vertex AI REST DELETE for Runtime `382095134059134976`: HTTP 200, completed operation `8675603336830910464`.
- Terraform apply in `20260822-agent-gateway/terraform`:
  `terraform apply -input=false /tmp/common-migration-20260822-destroy-final.tfplan`
- Terraform result: `Apply complete! Resources: 0 added, 0 changed, 9 destroyed.`
- Agent Gateway deletion completed after 4m37s.

## Post-cleanup verification

- `terraform state list` in `20260822-agent-gateway/terraform`: empty.
- `gcloud network-services agent-gateways list --project=nnyn-dev --location=us-central1`: no rows.
- Vertex AI `reasoningEngines.list` in `nnyn-dev/us-central1`: neither approved Runtime ID was returned.
- The eight `google_project_service` state entries had `disable_on_destroy=false`; the relevant APIs remain enabled after state cleanup.
- No consumer Terraform plan or apply was run during this cleanup.

## Ownership migration checks

- `20260822-agent-gateway/terraform` contains no Agent Gateway, authorization,
  Runtime, Registry, or consumer resource definitions; its state is empty.
- `20260823-mcp-server/terraform` contains no Agent Gateway resource or
  Gateway-name variable. `agent_gateway_id` is a required, validated input.
- Consumer checks run without plan/apply: both Terraform configurations passed
  `validate`; common static validation passed; MCP `npm test` passed (7 tests)
  and `npm run check:tool-spec` passed.
- The consumer repositories were not redeployed or reapplied during this
  migration.

## Common deployment plan

- Saved plan: `/tmp/common-agent-gateway-final.tfplan`.
- Root/state: `common/terraform`, empty local state before planning.
- Scope: project `nnyn-dev`, region `us-central1`, Gateway name
  `common-agent-gateway-egress`, subnet CIDR `10.243.0.0/28`.
- Plan result: `8 to add, 0 to change, 0 to destroy`.
- Planned creates: four API-enablement state entries, dedicated VPC,
  dedicated subnet, Network Attachment, and VPC-connected Agent Gateway.
- JSON inspection confirmed no Runtime, Registry service, MCP server, GKE,
  Cloud Run, Artifact Registry, or consumer-specific IAM resource.
- The common plan has not been applied.

## Common live deployment verification

- Terraform apply result: `8 added, 0 changed, 0 destroyed`.
- Terraform outputs:
  - Gateway: `projects/nnyn-dev/locations/us-central1/agentGateways/common-agent-gateway-egress`
  - VPC: `projects/nnyn-dev/global/networks/common-agent-gateway-vpc`
  - subnet: `projects/nnyn-dev/regions/us-central1/subnetworks/common-agent-gateway-subnet`
  - Network Attachment: `https://www.googleapis.com/compute/v1/projects/nnyn-dev/regions/us-central1/networkAttachments/common-agent-gateway-attachment`
- Gateway describe returned `networkConfig.egress.networkAttachment` equal to
  the common Network Attachment URI, with `AGENT_TO_ANYWHERE` and `MCP`.
- Network Attachment describe returned the common subnet URI and
  `connectionPreference=ACCEPT_AUTOMATIC`.
- Subnet describe returned the common VPC URI, `10.243.0.0/28`, and
  `us-central1`.
- VPC describe returned `autoCreateSubnetworks=false` and
  `routingMode=REGIONAL`.
- The common Terraform state contains only four infrastructure resources and
  four required API state entries; no Runtime, Registry service, MCP server,
  GKE, Cloud Run, Artifact Registry, or consumer IAM resource is present.

## Final validation

- `openspec validate centralize-agent-gateway-in-common --strict`: valid.
- `common/scripts/validate.sh`: passed.
- Common post-apply `terraform plan -detailed-exitcode`: `No changes`.
- `20260822-agent-gateway` and `20260823-mcp-server` format/validate checks:
  passed; neither directory owns a shared Gateway.
- MCP `npm test`: 2 files and 7 tests passed.
- MCP `npm run check:tool-spec`: passed.
- No consumer Terraform plan or apply was run after the migration cleanup.
