## 1. Python プロジェクトとランタイム基盤

- [ ] 1.1 FastAPI、Uvicorn、HTTP クライアント、Google Cloud クライアント、pytest 用の依存関係と実行エントリポイントを `pyproject.toml` に定義し、ロックファイルを更新する
- [ ] 1.2 `verification_id` を含む入力モデル、許可する4操作、単項・ストリームの操作グループを定義する
- [ ] 1.3 `query` と `async_query` が非ブロッキングに10秒待機して `OK` を返すダミーエージェントを実装する
- [ ] 1.4 `stream_query` と `async_stream_query` が5秒時点で `Streaming OK`、10秒時点で `OK` を順次生成するダミーエージェントを実装する

## 2. Agent Platform HTTP 契約とログ

- [ ] 2.1 `/api/reasoning_engine` で `query` と `async_query` をディスパッチし、出力を単一 JSON 応答へ変換する
- [ ] 2.2 `/api/stream_reasoning_engine` で `stream_query` と `async_stream_query` をディスパッチし、出力を逐次 NDJSON として返す
- [ ] 2.3 未対応操作、誤ったエンドポイント、無効な入力を安全な4xx応答にし、入力本文や認証情報を露出しないエラー処理を実装する
- [ ] 2.4 全 HTTP リクエストの受信・完了と、クエリの開始・チャンク生成・終了・失敗を `request_id`、`verification_id`、操作名、時刻、経過時間付きの1行 JSON として標準出力へ記録する

## 3. ローカル自動テストとコンテナ検証

- [ ] 3.1 注入可能な待機・時計を使い、4メソッドの応答内容、順序、待機タイミング、非ブロッキング性を高速なユニットテストで検証する
- [ ] 3.2 FastAPI の2エンドポイントについて単項 JSON、逐次 NDJSON、操作ルーティング、4xx、ライフサイクルログを結合テストで検証する
- [ ] 3.3 非 root ユーザーで `0.0.0.0:8080` に起動する Dockerfile と `.dockerignore` を追加し、コンテナイメージをビルドする
- [ ] 3.4 ローカルコンテナへ4操作を実時間で実行し、期待応答、順序、経過時間と受信・開始・終了ログを照合するスモークテストを完了する

## 4. ローカル検証クライアント

- [ ] 4.1 ローカルおよびデプロイ済みエージェントを対象に、4操作を個別または一括実行できる CLI を実装する
- [ ] 4.2 CLI が試行ごとの一意な `verification_id`、対象、操作、各応答、受信順序、経過時間、成否を JSON Lines で出力するようにする
- [ ] 4.3 Agent Platform SDK の同期・非同期呼び出しと、切り分け用の認証付き `:query`・`:streamQuery` REST 呼び出しを実装しテストする

## 5. Google Cloud デプロイ基盤

- [ ] 5.1 Terraform で必要 API、検証専用 Artifact Registry、ランタイムサービスアカウント、最小限の IAM と長時間ジョブ用 GCS バケットを定義し、機密値を含まない設定例を追加する
- [ ] 5.2 Terraform の format、validate、plan を実行し、作成対象と権限が検証専用の範囲であることを確認する
- [ ] 5.3 Docker build、Artifact Registry 認証、push を行う再実行可能なスクリプトを追加する
- [ ] 5.4 `query`=`""`、`async_query`=`"async"`、`stream_query`=`"stream"`、`async_stream_query`=`"async_stream"` の `classMethods` を宣言してエージェントを作成し、リソース名を保存するスクリプトを追加する
- [ ] 5.5 デプロイ状態と operation schema を取得する手順、および対象エージェントを明示指定して削除するスクリプトを追加する

## 6. デプロイ済みエージェントのライブ検証

- [ ] 6.1 検証専用イメージと Agent Platform エージェントをデプロイし、稼働状態と4つの operation schema を保存する
- [ ] 6.2 デプロイ済みエージェントへ4操作を実行し、応答内容、順序、クライアント経過時間を結果ファイルへ保存する
- [ ] 6.3 各 `verification_id` について Cloud Logging から受信・開始・チャンク・終了ログを取得し、クライアント結果と照合する
- [ ] 6.4 SDK と REST で結果が異なる場合は両経路の要求・応答・対応ログを記録し、到達箇所を切り分ける

## 7. 長時間実行クエリジョブ検証

- [ ] 7.1 一意な検証マーカーと GCS 出力先で `run_query_job` を開始し、ジョブ名、開始時刻、入力、出力 URI を保存する CLI を実装する
- [ ] 7.2 `check_query_job` を指定間隔・指定期限で監視し、全状態遷移と最終結果または期限超過を追記する
- [ ] 7.3 ジョブ出力と、開始前からログ取り込み猶予後までの Cloud Logging を収集し、検証マーカーと対象エージェントで検索する
- [ ] 7.4 収集証跡から「到達確認」「未到達」「判定不能」を判定し、過去 PoC の事象との一致・相違を理由付きで結果文書へ記録する
- [ ] 7.5 必要な場合は REST `:asyncQuery` の GCS 入力方式でも追加検証し、SDK 経路と分けて結果を記録する

## 8. 文書化と後片付け

- [ ] 8.1 README に前提条件、ローカル検証、デプロイ、4操作の実行、ログ確認、長時間ジョブ、結果の読み方を記載する
- [ ] 8.2 認証情報や機密入力を含まない検証結果を整理し、実行日時、SDK バージョン、対象リソース、公式ドキュメント確認日を残す
- [ ] 8.3 検証用エージェントを削除し、不要な Terraform 管理リソースを削除して残存リソースを確認する
