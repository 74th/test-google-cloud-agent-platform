## 1. IAM とデプロイ前提条件

- [x] 1.1 Terraform で専用実行サービスアカウントへ `roles/serviceusage.serviceUsageConsumer` を付与し、`terraform fmt -check` と `terraform validate` が成功することを確認する
- [x] 1.2 Terraform plan を取得し、IAM差分が対象サービスアカウントへの意図したロール追加であり、既存のバケット権限や他リソースを置換しないことを確認する
- [x] 1.3 長時間ジョブ実行前に GCS オブジェクト権限と `serviceusage.services.use` の不足を判別できる前提確認を追加し、権限あり・なしのテストで診断結果を確認する

## 2. 長時間ジョブ用ランタイム契約

- [x] 2.1 長時間ジョブ入力に省略可能な `delay_seconds` と安全な上限を追加し、既定値・960秒・負数・上限超過のモデルテストを通す
- [x] 2.2 DummyAgent の非ストリーミング処理が検証入力の遅延時間を非ブロッキングに使用できるようにし、注入した sleep を使う単体テストで待機値と応答を確認する
- [x] 2.3 `POST /` を長時間ジョブ用入力の正規化境界として分離し、操作名なしの GCS 入力を `query` として処理する一方、`/api/reasoning_engine` の既存契約を維持するテストを通す
- [x] 2.4 ルートへの有効な長時間ジョブ入力が200と `{"output":"OK"}` を返し、欠落入力・未対応操作・不正な遅延時間が4xxになるエンドポイントテストを通す
- [x] 2.5 通常4操作の既存テストを実行し、応答内容、ストリーム順序、操作名必須の挙動に回帰がないことを確認する

## 3. 長時間ジョブの証跡収集と判定

- [x] 3.1 query-job CLI に遅延時間指定を追加し、生成する GCS 入力とローカルテストが既定10秒・指定960秒を正しく表現することを確認する
- [x] 3.2 `run_query_job` 応答から実際の `input_gcs_uri` と `output_gcs_uri` を保存し、SDKがファイル名またはディレクトリ名を書き換えるケースのテストを通す
- [x] 3.3 状態確認と operation 取得から成功結果または最終エラーコード・安全化したメッセージを保存し、FAILED operation の fixture でコンテナ終了エラーを保持できることを確認する
- [x] 3.4 Cloud Logging 検索を `jsonPayload.event`、HTTPアクセスログ、severity、`proxy-container`、`job-container` を対象に更新し、構造化イベントとproxyのGCSエラーを同じ時間範囲から取得するテストを通す
- [x] 3.5 ログ要約でコンテナ名、処理段階、HTTPステータス、エラー種別、権限名だけを保持し、入力本文、Authorization、URLクエリを出力しないセキュリティテストを通す
- [x] 3.6 評価ロジックを GCS入力取得、HTTP配送、アプリ処理、operation終端、GCS出力の段階へ拡張し、IAM拒否・422・処理失敗・全成功・証跡不足の各fixtureで期待する判定を確認する
- [x] 3.7 監視期限超過時のキャンセルを明示オプションにし、15分超モードでは処理時間プラス10分以上を監視して自動キャンセルしないCLIテストを通す

## 4. ローカルとコンテナの統合検証

- [x] 4.1 全Pythonテストを実行し、長時間ジョブ追加ケースを含めて `pytest` が成功することを確認する
- [x] 4.2 検証イメージをローカルでビルド・起動し、GCS入力相当の `POST /` と通常4操作が期待するHTTP応答とライフサイクルログを返すことを確認する
- [x] 4.3 960秒を実時間待機しないテスト用clock/sleepで15分超入力の処理経路を検証し、上限検証と非ブロッキング呼び出しを確認する

## 5. Google Cloud 上の段階検証

- [x] 5.1 Terraform IAM変更を検証プロジェクトへ適用し、実行サービスアカウントに Service Usage Consumer と対象バケットのオブジェクト権限が付与されたことをIAMポリシーで確認する
- [x] 5.2 新しいイメージを検証専用エージェントへデプロイし、通常4操作がすべて成功することをクライアント応答とコンテナログで確認する
- [x] 5.3 約10秒のGCS長時間ジョブを1件実行し、proxyの入力取得、`POST /` の200、SUCCESS終端、実出力オブジェクト、`{"output":"OK"}` を同じ試行IDで確認する
- [x] 5.4 5.3の全段階成功をゲートとして960秒のジョブを1件実行し、15分を超えてからSUCCESS終端し、GCS出力と対応ログが存在することを確認する
- [x] 5.5 SDK経路と同じGCS入力をREST `:asyncQuery` でも実行し、operation名、配送先 `/`、出力形式に差がないことを非機密の比較結果へ記録する

## 6. 文書化と後片付け

- [x] 6.1 README に必要IAM、GCS入力形式、短時間ゲート、15分超実行、実URIとproxy/jobログの確認方法を追加し、記載コマンドを検証環境で実行できることを確認する
- [x] 6.2 2026-08-21/22の「未到達」をGCS取得時の `serviceusage.services.use` 拒否として再評価し、認証情報や入力本文を含まない結果文書へ根拠と新しい検証結果を記録する
- [x] 6.3 検証用ジョブ、エージェント、不要なGCSオブジェクトを対象名を確認して削除し、Terraform管理IAMを含む残存リソースを一覧で確認する
- [x] 6.4 `openspec validate enable-byoc-long-running-query --strict` を実行し、proposal・specs・design・tasks と実装結果の整合性を確認する
