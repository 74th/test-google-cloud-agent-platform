## 1. ルートエンドポイント実装

- [x] 1.1 既存の単項ハンドラーを `POST /` と `POST /api/reasoning_engine` の両方へ登録し、入力検証・許可操作・応答・ログ処理を共有する
- [x] 1.2 同じ有効入力に対して2つの単項パスの状態コードと応答が一致する HTTP テストを追加する
- [x] 1.3 ルートパスの無効入力が安全な 4xx と受信・完了ログを生成し、入力本文や秘密値をログへ出さないことをテストする
- [x] 1.4 既存の単項・ストリーム・エージェントテスト一式を実行し、通常の4操作に回帰がないことを確認する

## 2. 長時間ジョブの証跡収集と判定

- [x] 2.1 長時間ジョブの試行記録に対象エージェント、試行 ID、ジョブ名、開始・終了時刻、監視期限、出力 URI、ログ検索範囲を保存する
- [x] 2.2 対象リソースと時間範囲から `POST /` の HTTP 受信・完了ログおよび対応する処理イベントを収集し、リクエスト ID と検証マーカーで関連付ける
- [x] 2.3 GCS 出力の有無と機密情報を除いた結果概要を収集し、ジョブの状態遷移とともに同じ JSON Lines 結果へ保存する
- [x] 2.4 `http_delivery`、`processing`、`job_terminal_state`、`gcs_output` を段階別に評価し、総合結果を「動作確認」「配送確認・動作未確認」「未到達」「判定不能」から決定する
- [x] 2.5 クラウド接続をスタブ化したテストで、成功、ルート到達後の失敗、未到達、証跡不足の各判定と秘密値を含まない出力を検証する

## 3. ローカルおよびコンテナ検証

- [x] 3.1 ローカル FastAPI に `POST /` の単項要求を送り、期待する応答と `path=/` の処理ライフサイクルログを確認する
- [x] 3.2 新しいコンテナイメージをローカルで build・起動し、ルートパスと既存4操作のスモークテストを実施する
- [x] 3.3 ローカル検証のコマンド、期待結果、および安全なログ確認方法を README に追記する

## 4. Agent Platform ライブ検証

- [x] 4.1 前回の検証基盤が `terraform destroy` 済みであることを前提に、`terraform.tfvars` の環境固有値を設定し、`terraform init`、`terraform fmt -check`、`terraform validate`、`terraform plan` を実行する
- [x] 4.2 plan が検証専用の Artifact Registry、サービスアカウント、IAM、GCS バケットなどだけを再作成することを確認して `terraform apply` を実行し、出力値と作成リソースを保存する
- [x] 4.3 Terraform で再作成した Artifact Registry へ一意なイメージタグでコンテナを build・push し、新規の検証専用エージェントへデプロイした image URI、revision、実行日時を保存する
- [x] 4.4 デプロイ後に通常の4操作を実行し、既存の応答、順序、コンテナログに回帰がないことを確認する
- [x] 4.5 他の長時間ジョブを並行実行せず、一意な検証マーカーと Terraform で再作成した専用 GCS 出力先で長時間実行クエリジョブを1試行開始する
- [x] 4.6 ジョブ終端または監視期限まで状態を追跡し、ログ取り込み猶予後にルート受信・処理ログと GCS 出力を収集する
- [x] 4.7 段階別証跡から総合結果を判定し、以前の「判定不能」との差異、SDK・イメージ・対象リソース・検索条件を機密情報なしで結果文書へ記録する

## 5. 文書化と後片付け

- [x] 5.1 README の長時間ジョブ手順と結果の読み方を、ルートエンドポイントと4段階の判定に合わせて更新する
- [x] 5.2 検証用エージェントを削除し、`terraform destroy` で今回再作成した Terraform 管理リソースを削除して残存リソースを確認する
