## 1. ランタイムとスキルの準備

- [x] 1.1 選択した `nnyn-dev` のロケーションについて、現行の Google Cloud Agent Platform コンテナランタイム、呼び出し契約、必要な API、サポートされるデプロイコマンドを確認する。
- [x] 1.2 Claude Agent SDK ランタイムアダプターとその自動テスト用の Python 依存関係およびプロジェクト構成を追加する。
- [x] 1.3 `workspace/.claude/skills/bus-schedule/SKILL.md` を唯一の参照元として、架空の全路線、既定の金沢さくら台三丁目から金沢みらい駅への動作、Asia/Tokyo での次の出発便の判定、所要約25分の金沢テスト病院経由便への注意を含む `金沢テストバス時刻表` スキルをコンテナへ取り込む。
- [x] 1.4 テキストリクエストを検証し、パッケージ化したスキルとともに Claude Agent SDK を呼び出し、正規化されたテキスト応答または対処可能な失敗を返すエージェントランタイムアダプターを実装する。
- [x] 1.5 `workspace/.claude/skills/bus-schedule/SKILL.md` がコンテナへ取り込まれること、およびアダプターのリクエスト／レスポンス検証のユニットテストを追加する。架空の直通便と所要時間の長い経由便の時刻表応答、以降の便がない場合を含める。
- [x] 1.6 Claude Agent SDK を Vertex AI モードへ設定し、`ClaudeAgentOptions` に Vertex AI の `claude-haiku-4-5@20251001` を明示するテストを追加する。

## 2. コンテナとデプロイの自動化

- [x] 2.1 スキルが含まれ、認証情報がイメージに埋め込まれないことを保証する、Google Cloud Agent Platform で必要なコンテナ定義およびランタイム設定を作成する。
- [x] 2.2 プロジェクト、ロケーション、アーティファクト、ランタイムのデプロイ先、シークレット参照の入力を検証する、コミット対象の設定例およびデプロイスクリプトを追加する。プロジェクトの既定値は `nnyn-dev` とする。
- [x] 2.3 検証済みのプラットフォーム契約を用い、ビルド、イメージ公開、Agent Platform デプロイ、デプロイ識別子またはエンドポイントの出力をデプロイスクリプトに実装する。
- [x] 2.4 スクリプトで作成したデプロイリソース用に、文書化されたクリーンアップまたはロールバックコマンドを追加する。
- [x] 2.5 設定不足時のデプロイスクリプト検証経路をテストし、API キーがログ出力もコミットもされないことを確認する。
- [x] 2.6 `us-central1` を既定ロケーションとする Terraform 構成を更新し、Agent Platform コンテナ専用サービスアカウントを作成する。
- [x] 2.7 Terraform に、ランタイムサービスアカウントへの Vertex AI 推論に必要な最小権限を追加し、Claude API キーまたは Secret Manager 読み取り権限を削除する。
- [x] 2.8 デプロイスクリプトが Terraform output の専用サービスアカウントを必須入力として Agent Platform へ渡し、Vertex AI のプロジェクト・推論リージョン・固定モデル設定を注入するように、設定例と文書を更新する。
- [x] 2.9 Terraform の format/validate とサービスアカウント／IAM／API キー非露出を確認するテストを更新する。
- [x] 2.10 `us-central1` のテスト用 Docker Artifact Registry リポジトリ `agent-hosting-20260801`、Artifact Registry API、有効な Agent Runtime サービスエージェントのリポジトリ読み取り権限を Terraform で管理し、デプロイスクリプト・設定例・テストを Terraform output 利用へ更新する。
- [x] 2.11 Cloud Build API と Cloud Build のデプロイ経路を削除し、ローカル Docker によるビルド、Artifact Registry 認証、イメージ push の経路とテストを追加する。

## 3. ローカル呼び出しとドキュメント

- [x] 3.1 文書化された Google Cloud 認証情報を取得し、デプロイ識別子またはエンドポイントとプロンプトを受け取り、Agent Platform サービスを呼び出して、正規化された応答のみを出力するローカル Python CLI を実装する。
- [x] 3.2 デプロイ先設定不足、認証または通信の失敗、不正なサービスレスポンスに対するローカルクライアントのエラー処理とテストを追加する。
- [x] 3.3 前提条件、Vertex AI のモデル有効化、サービスアカウント認証、デプロイ変数、デプロイ／ロールバックコマンド、ローカル呼び出し手順を文書化する。
- [x] 3.4 `次のバスは何時？` をライブスモークテストとして、また決定的な時刻表アサーションのための制御時刻テスト手法として文書化する。

## 4. エンドツーエンド検証

- [x] 4.1 エージェントコンテナをビルドし、クラウドへデプロイする前にサポートされたローカルまたは契約レベルのチェックを実行する。
- [x] 4.2 `us-central1` と Terraform で作成したサービスアカウント／Artifact Registry リポジトリ、および Vertex AI で有効化した Claude Haiku 4.5 を用いて、`nnyn-dev` の設定済み Agent Platform 環境にデプロイする（`projects/776113568960/locations/us-central1/reasoningEngines/4788401176510988288`）。
- [x] 4.3 `次のバスは何時？` を指定してデプロイ済みエージェントに対するローカル Python スモークテストを実行し、応答が表示されることを確認する。
- [x] 4.4 制御された時刻または該当時刻で、所要時間の長い兼六・ひがし茶屋コースへの回答に出発／到着時刻と金沢テスト病院経由／約25分の注意がともに含まれることを確認し、結果を記録する（2026-08-01 11:06 JST、11:35 発・12:00 着、金沢テスト病院経由・約25分）。
