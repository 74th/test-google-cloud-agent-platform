## 1. ランタイムとスキルの準備

- [ ] 1.1 選択した `nnyn-dev` のロケーションについて、現行の Google Cloud Agent Platform コンテナランタイム、呼び出し契約、必要な API、サポートされるデプロイコマンドを確認する。
- [ ] 1.2 Claude Agent SDK ランタイムアダプターとその自動テスト用の Python 依存関係およびプロジェクト構成を追加する。
- [ ] 1.3 `workspace/.claude/skills/bus-schedule/SKILL.md` を唯一の参照元として、架空の全路線、既定の金沢さくら台三丁目から金沢みらい駅への動作、Asia/Tokyo での次の出発便の判定、所要約25分の金沢テスト病院経由便への注意を含む `金沢テストバス時刻表` スキルをコンテナへ取り込む。
- [ ] 1.4 テキストリクエストを検証し、パッケージ化したスキルとともに Claude Agent SDK を呼び出し、正規化されたテキスト応答または対処可能な失敗を返すエージェントランタイムアダプターを実装する。
- [ ] 1.5 `workspace/.claude/skills/bus-schedule/SKILL.md` がコンテナへ取り込まれること、およびアダプターのリクエスト／レスポンス検証のユニットテストを追加する。架空の直通便と所要時間の長い経由便の時刻表応答、以降の便がない場合を含める。

## 2. コンテナとデプロイの自動化

- [ ] 2.1 スキルが含まれ、認証情報がイメージに埋め込まれないことを保証する、Google Cloud Agent Platform で必要なコンテナ定義およびランタイム設定を作成する。
- [ ] 2.2 プロジェクト、ロケーション、アーティファクト、ランタイムのデプロイ先、シークレット参照の入力を検証する、コミット対象の設定例およびデプロイスクリプトを追加する。プロジェクトの既定値は `nnyn-dev` とする。
- [ ] 2.3 検証済みのプラットフォーム契約を用い、ビルド、イメージ公開、Agent Platform デプロイ、デプロイ識別子またはエンドポイントの出力をデプロイスクリプトに実装する。
- [ ] 2.4 スクリプトで作成したデプロイリソース用に、文書化されたクリーンアップまたはロールバックコマンドを追加する。
- [ ] 2.5 設定不足時のデプロイスクリプト検証経路をテストし、シークレット値がログ出力もコミットもされないことを確認する。

## 3. ローカル呼び出しとドキュメント

- [ ] 3.1 文書化された Google Cloud 認証情報を取得し、デプロイ識別子またはエンドポイントとプロンプトを受け取り、Agent Platform サービスを呼び出して、正規化された応答のみを出力するローカル Python CLI を実装する。
- [ ] 3.2 デプロイ先設定不足、認証または通信の失敗、不正なサービスレスポンスに対するローカルクライアントのエラー処理とテストを追加する。
- [ ] 3.3 前提条件、Claude シークレット設定、Google Cloud 認証、デプロイ変数、デプロイ／ロールバックコマンド、ローカル呼び出し手順を文書化する。
- [ ] 3.4 `次のバスは何時？` をライブスモークテストとして、また決定的な時刻表アサーションのための制御時刻テスト手法として文書化する。

## 4. エンドツーエンド検証

- [ ] 4.1 エージェントコンテナをビルドし、クラウドへデプロイする前にサポートされたローカルまたは契約レベルのチェックを実行する。
- [ ] 4.2 運用者が指定したロケーションとシークレット参照を用いて、`nnyn-dev` の設定済み Agent Platform 環境にデプロイする。
- [ ] 4.3 `次のバスは何時？` を指定してデプロイ済みエージェントに対するローカル Python スモークテストを実行し、応答が表示されることを確認する。
- [ ] 4.4 制御された時刻または該当時刻で、所要時間の長い兼六・ひがし茶屋コースへの回答に出発／到着時刻と金沢テスト病院経由／約25分の注意がともに含まれることを確認し、結果を記録する。
